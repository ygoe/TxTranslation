<UserControl
	x:Class="TxEditor.View.CultureTextView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:my="clr-namespace:TxEditor"
	xmlns:v="clr-namespace:TxEditor.View" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	mc:Ignorable="d" 
	d:DesignHeight="214" d:DesignWidth="443">
	<StackPanel>
		<DockPanel>
			<Button DockPanel.Dock="Right" Padding="6,0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">1</Button>
			<Button DockPanel.Dock="Right" Padding="6,0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">0</Button>
			<TextBlock Opacity="0.5" Margin="1,0" Text="{Binding CultureNativeName}"/>
		</DockPanel>
		<my:DecoratedTextBox BorderThickness="0" Padding="0" Margin="-1,6,0,0" Text="{Binding Text}"/>
		<ItemsControl ItemsSource="{Binding QuantifiedTextVMs}">
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<v:QuantifiedTextView Margin="0,6,0,0"/>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
		<Line X2="1" Stretch="Fill" Stroke="#40000000" Margin="0,6"/>
	</StackPanel>
</UserControl>
