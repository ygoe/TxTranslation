<UserControl x:Class="TxEditor.DecoratedTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
			 Name="root"
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid Name="grid1">
		<TextBox
			Name="textBox1" AcceptsReturn="True"
			BorderThickness="{Binding BorderThickness, ElementName=root}"
			Padding="{Binding Padding, ElementName=root}"
			Text="{Binding Text, ElementName=root}"
			TextChanged="textBox1_TextChanged" SelectionChanged="textBox1_SelectionChanged" PreviewKeyDown="textBox1_KeyDown"
			TextWrapping="Wrap"
			Background="Transparent" AcceptsTab="True" VerticalScrollBarVisibility="Auto" />
	</Grid>
</UserControl>
